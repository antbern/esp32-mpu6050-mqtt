const char* ssid     = "SSID";
const char* password = "PASSWORD";

const char* mqtt_server = "SERVER IP/HOSTNAME";
const int mqtt_port = 1883;        // use 8883 for SSL
const char* mqtt_device_name = "esp32 device name";